version: 1
backend:
  phases:
    build:
      commands:
        - echo "No backend build"
frontend:
  phases:
    preBuild:
      commands:
        - cd website && npm ci
    build:
      commands:
        - cd website && npm run build
  artifacts:
    baseDirectory: website
    files:
      - '.next/**/*'
      - 'public/**/*'
      - 'package.json'
  cache:
    paths:
      - website/node_modules/**/*
