version: 1
backend:
  phases:
    build:
      commands:
        - echo "No backend build"
frontend:
  phases:
    build:
      commands:
        - bash build.sh
  artifacts:
    baseDirectory: website/out
    files:
      - '**/*'
  cache:
    paths:
      - website/node_modules/**/*
