# Workspace Hub Access Configuration
# Generated: 2026-02-15
# Workspace: ima_systems

# Primary hub endpoint (all workspaces connect through this)
PRIMARY_HUB_URL=http://localhost:8000

# This workspace's direct hub endpoint
COLLABORATION_HUB_URL=http://localhost:8015

# Workspace identifier
WORKSPACE_NAME=ima_systems

# Agent instance type (copilot, specialist, expert)
AGENT_INSTANCE_TYPE=copilot

# Access token for hub authentication
ACCESS_TOKEN=ws_8925184f31c04be52d62ec212753e558

# Heartbeat interval (seconds)
HEARTBEAT_INTERVAL=30

# Automatic reconnection on disconnect
AUTO_RECONNECT=true

# Reconnection timeout (seconds)
RECONNECT_TIMEOUT=10

# Sync mode (async, sync)
SYNC_MODE=async

# Maximum retry attempts for hub connection
MAX_RETRIES=3

# Debug mode
DEBUG=false
